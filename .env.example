# JWT Configuration for Production
# IMPORTANT: Change these values for production deployment

# Secret key for JWT token signing (use a strong random string)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=change-me-in-production-with-strong-secret

# JWT token expiration time in hours (default: 24)
JWT_EXPIRATION_HOURS=24

# User Credentials
# Users are stored in users.json with hashed passwords
# Add users by running: python -c "from backend.app.auth import UserStore; u = UserStore('backend/users.json'); u.add_user('admin', 'password123'); print('User added')"

